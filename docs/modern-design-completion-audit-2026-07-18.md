# Аудит современного дизайна — 18.07.2026

## Статус

Код, веб-проверка, Android-синхронизация и сборка APK завершены. Проверка непосредственно на телефоне ожидает подключения устройства: команда `adb devices -l` вернула пустой список.

## Закрытые требования

1. Оставлена одна тёмная графитово-лаймовая тема.
   - Переключатель тем удалён.
   - Альтернативные цветовые темы удалены из CSS.
   - При загрузке принудительно используется `theme='dark'`.

2. Первый запуск перестроен.
   - Добавлен короткий двухшаговый сценарий.
   - Собираются вес, рост, возраст, пол, образ жизни и цель питания.
   - Данные сохраняются в профиль и сразу участвуют в расчётах.

3. Навигация пересмотрена.
   - Нижние разделы: «Тренировка», «Программа», «Прогресс», «Профиль», «Ещё».
   - Профиль и программа вынесены из технических настроек.
   - В «Ещё» оставлены техника счёта, камеры, звук, язык и переходы к вариантам отжиманий и галерее.

4. Расписание переработано.
   - «Дни тренировок» отображаются отдельной подписью.
   - Все семь дней находятся в одной строке.
   - На главной дни увеличены и получили более выраженные состояния.

5. Главная исправлена.
   - Круг дневного прогресса поднят на 22 px внутри собственной зоны.
   - Отрицательное наложение зоны героя на карточку убрано.
   - Для мужчины используются `aspect-ratio: 2/3`, `object-fit: contain` и `height: auto`.
   - Главная визуально проверена при размере 390 × 844 px.

6. Названия камер исправлены.
   - Используются «Передняя камера» и «Основная камера».
   - Строки «Фронт» в интерфейсе отсутствуют.

7. Экран прогресса и галерея переработаны.
   - Действия прогресса получили отдельную сетку без некрасивых переносов.
   - Фото- и видео-действия собраны в аккуратную адаптивную панель.
   - Визуально проверены экран прогресса и пустая галерея.

8. Достижения улучшены.
   - Добавлены 19 крупных карточек.
   - Каждая карточка показывает название, условие, дату/состояние и текущий прогресс.
   - По нажатию открывается отдельный экран подробностей.
   - Даты получения сохраняются в `pushup_achieve_dates` и входят в экспорт/импорт.

9. Старые маскоты удалены.
   - `cat-coach-v1.png` удалён из веб- и Android-ассетов.
   - Ссылок на Барса и чёрных пантер в интерфейсе нет.
   - В собранном APK старых маскотов нет.

10. Лайма и Лайм подключены.
    - Выбор выполняется крупными карточками с изображениями.
    - Выбор сохраняется как `coachId`.
    - Выбранный тренер используется в подсказках, прогрессе и разделе «Мой тренер».
    - Офлайн-помощник не называется AI/«ИИ-тренером».

11. Карточки техники обновлены.
    - Добавлен фон `technique-gym-bg-v1.png`: ночной городской зал, бетон, металл, кольца, канаты и лаймовые акценты.
    - Текст и схемы накладываются через HTML/CSS.
    - В отдельном основном блоке присутствуют: классические, с колен, от стены, от опоры, алмазные, широкие, «Лучник», ноги на возвышении, пайк и сфинкс.
    - В каталоге сохраняются дополнительные варианты.

12. Уведомления унифицированы.
    - В коде отсутствуют вызовы `alert()` и `confirm()`.
    - Добавлен единый модальный компонент для информации, успеха, предупреждений, ошибок и подтверждений.
    - Важные сообщения остаются на экране до действия пользователя.

## Проверки

- Синтаксис JavaScript: успешно.
- `git diff --check`: ошибок пробелов нет.
- Ошибки консоли при браузерной проверке: отсутствуют.
- Мобильная проверка: 390 × 844 px.
- Навигация: пять новых разделов отображаются и открываются.
- Расписание: семь колонок в одной строке.
- Достижения: 19 карточек, экран подробностей открывается.
- Основные карточки техники: все 10 требуемых названий присутствуют.
- Стандартные `alert/confirm`: 0.
- Альтернативные темы: 0 элементов и 0 CSS-блоков.
- Старые маскоты в APK: отсутствуют.

## Android и APK

- Выполнено: `npx.cmd cap sync android`.
- Выполнено: `gradlew --no-daemon assembleDebug --rerun-tasks --console=plain`.
- APK: `android/app/build/outputs/apk/debug/app-debug.apk`.
- Размер: `40 763 778` байт.
- SHA-256: `698789BF346558BBAC7CAE4D37A3F167862883F7FC8A206C29429AE1846A8C87`.
- В APK подтверждены:
  - `assets/public/index.html`;
  - `coach-layma-fox-v1.png`;
  - `coach-lime-fox-v1.png`;
  - `hero-man-pushup-v1.png`;
  - `technique-gym-bg-v1.png`.

## Проверка на реальном телефоне

- Устройство: Xiaomi `2412DPC0AG`, разрешение экрана 1220 × 2712 px.
- ADB-состояние: `device`.
- Debug-пакет `com.otzhimaniya.app.debug` установлен командой `adb install -r`: `Success`.
- Приложение запущено штатной launcher-активностью `com.otzhimaniya.app.MainActivity`.
- На устройстве визуально проверены главная, активная тренировка, программа, карточки вариантов техники и фирменное окно подтверждения.
- Главная подтверждает исправление героя: мужчина сохраняет пропорции; круг прогресса поднят и не перекрывает карточку.
- Нижняя навигация отображает пять разделов: «Тренировка», «Программа», «Прогресс», «Профиль», «Ещё».
- Экран вариантов показывает заголовок «Основные карточки техники · 10» и новые карточки на фоне ночного зала.
- Подтверждение удаления открывается внутри приложения в графитово-лаймовом дизайне, системный `alert/confirm` не используется.
- Во время запуска и переходов падений процесса `com.otzhimaniya.app.debug` не зафиксировано.

Все пункты исходного списка реализованы, Android-сборка синхронизирована, debug APK собран и проверен на реальном устройстве.
